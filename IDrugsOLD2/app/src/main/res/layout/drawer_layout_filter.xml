<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayoutLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout_filter"
    android:layout_width="216dp"
    android:layout_height="match_parent">

    <CheckBox
        android:checked="true"
        android:text="Adulto" />

    <CheckBox
        android:checked="true"
        android:text="Infantil" />

    <FrameLayout
        android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <ListView
        android:id="@+id/list_filter_drawer"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:choiceMode="singleChoice"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp"
        android:background="#111"/>

</android.support.v4.widget.DrawerLayoutLayout>
